// WorkoutCore
//
// Domain types and pure logic: value types, calculations, and the protocols that describe what
// the app needs from storage.
//
// This module imports Foundation and nothing else. No SwiftData, no SwiftUI. That constraint is
// what lets its tests run in milliseconds without a simulator, so treat any new import here as a
// design smell rather than a convenience.
//
// Intentionally empty — the data model follows the first feature rather than preceding it.
