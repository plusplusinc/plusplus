// Features
//
// Screens and their view models. Depends on DesignSystem for presentation and on WorkoutCore's
// protocols for data, never on WorkoutStore's concrete types.
//
// Features must not import each other. If two need the same thing, it belongs one layer down.
//
// Intentionally empty — awaiting the first feature.
